Name:           ros-hydro-slider-gui
Version:        1.0.3
Release:        0%{?dist}
Summary:        ROS slider_gui package

Group:          Development/Libraries
License:        BSD
URL:            http://ros.org/wiki/slider_gui
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-hydro-actionlib
Requires:       ros-hydro-face-detector
Requires:       ros-hydro-korg-nanokontrol
Requires:       ros-hydro-pr2-controllers-msgs
Requires:       ros-hydro-pr2-mechanism-msgs
Requires:       ros-hydro-program-queue
Requires:       ros-hydro-rospy
Requires:       ros-hydro-rviz-backdrop
Requires:       ros-hydro-sensor-msgs
Requires:       ros-hydro-sound-play
Requires:       ros-hydro-std-srvs
Requires:       ros-hydro-trajectory-msgs
BuildRequires:  ros-hydro-actionlib
BuildRequires:  ros-hydro-catkin
BuildRequires:  ros-hydro-face-detector
BuildRequires:  ros-hydro-korg-nanokontrol
BuildRequires:  ros-hydro-pr2-controllers-msgs
BuildRequires:  ros-hydro-pr2-mechanism-msgs
BuildRequires:  ros-hydro-program-queue
BuildRequires:  ros-hydro-rospy
BuildRequires:  ros-hydro-rviz-backdrop
BuildRequires:  ros-hydro-sensor-msgs
BuildRequires:  ros-hydro-sound-play
BuildRequires:  ros-hydro-std-srvs
BuildRequires:  ros-hydro-trajectory-msgs

%description
slider_gui

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/hydro/setup.sh" ]; then . "/opt/ros/hydro/setup.sh"; fi
mkdir -p build && cd build
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/hydro" \
        -DCMAKE_PREFIX_PATH="/opt/ros/hydro" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/hydro/setup.sh" ]; then . "/opt/ros/hydro/setup.sh"; fi
cd build
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/hydro

%changelog
* Wed Sep 10 2014 Devon Ash <dash@clearpathrobotics.com> - 1.0.3-0
- Autogenerated by Bloom

